(
    () =>{
        //basically do not use anymore var
        //always use const
        //const do not works like in other lenguages
        //const just doesn't let you reasignate the value, but you can mutate it
    }
)()