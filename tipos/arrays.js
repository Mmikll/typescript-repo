"use strict";
(() => {
    //const number: ( number | string | boolean )[] = [1,2,3,4,5,"6",7,8];
    const number = [1, 2, 3, 4, 5, 6, 7, 8];
    // number.push(true);
    // console.log(numbers)
    const villians = ["omega rojo", "dormamu", "duende verde"];
    villians.forEach((v) => console.log(v.toUpperCase()));
})();
